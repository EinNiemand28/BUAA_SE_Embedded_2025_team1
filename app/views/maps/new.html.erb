<div class="max-w-3xl mx-auto py-6 sm:px-6 lg:px-8">
  <!-- 页面头部 -->
  <div class="mb-6">
    <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
      <%= t('maps.titles.new') %>
    </h2>
    <p class="mt-1 text-sm text-gray-500">
      创建新的机器人导航地图
    </p>
  </div>

  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <%= form_with model: @map, class: "space-y-6" do |form| %>
        <% if @map.errors.any? %>
          <div class="rounded-md bg-red-50 p-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">
                  有 <%= pluralize(@map.errors.count, "个错误") %> 阻止保存：
                </h3>
                <div class="mt-2 text-sm text-red-700">
                  <ul class="list-disc list-inside space-y-1">
                    <% @map.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <!-- 基本信息 -->
        <div>
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">基本信息</h3>
          
          <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
            <div class="sm:col-span-2">
              <%= form.label :name, class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_field :name, 
                    class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm",
                    placeholder: "输入地图名称" %>
              <p class="mt-2 text-sm text-gray-500">为地图输入一个简短且有描述性的名称</p>
            </div>

            <div class="sm:col-span-2">
              <%= form.label :description, class: "block text-sm font-medium text-gray-700" %>
              <%= form.text_area :description, rows: 3,
                    class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm",
                    placeholder: "输入地图描述（可选）" %>
              <p class="mt-2 text-sm text-gray-500">添加关于这张地图的详细信息，如覆盖区域、用途等</p>
            </div>

            <div class="sm:col-span-2">
              <%= form.label :map_data_url, class: "block text-sm font-medium text-gray-700" %>
              <%= form.url_field :map_data_url,
                    class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm",
                    placeholder: "https://example.com/map_data.yaml" %>
              <p class="mt-2 text-sm text-gray-500">地图数据文件的URL（一般情况下不需要手动填写，自动建图任务会自动设置此字段）</p>
            </div>
          </div>
        </div>

        <!-- 地图文件上传 -->
        <div>
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">地图文件</h3>
          
          <div class="grid grid-cols-1 gap-6">
            <div>
              <%= form.label :map_image, "地图图像文件", class: "block text-sm font-medium text-gray-700" %>
              <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-md">
                <div class="space-y-1 text-center">
                  <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                    <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                  <div class="flex text-sm text-gray-600">
                    <label for="map_map_image" class="relative cursor-pointer bg-white rounded-md font-medium text-blue-600 hover:text-blue-500 focus-within:outline-none focus-within:ring-2 focus-within:ring-offset-2 focus-within:ring-blue-500">
                      <span>上传文件</span>
                      <%= form.file_field :map_image, id: "map_map_image", class: "sr-only", accept: "image/*" %>
                    </label>
                    <p class="pl-1">或拖拽文件到此处</p>
                  </div>
                  <p class="text-xs text-gray-500">PNG, JPG, GIF 最大 10MB</p>
                </div>
              </div>
              <p class="mt-2 text-sm text-gray-500">上传ROS格式的地图图像文件(.pgm)或其他支持的图像格式</p>
            </div>
          </div>
        </div>

        <!-- 隐藏字段 -->
        <%= form.hidden_field :created_by_user_id, value: Current.user.id %>

        <!-- 自动建图提示 -->
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-blue-800">自动建图</h3>
              <div class="mt-2 text-sm text-blue-700">
                <p>您也可以通过创建自动建图任务来生成新地图。机器人会自动探索环境并创建地图。</p>
                <div class="mt-2">
                  <%= link_to "创建建图任务", control_robots_path, 
                        class: "font-medium text-blue-600 hover:text-blue-500" %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 表单按钮 -->
        <div class="flex justify-end space-x-3">
          <%= link_to "取消", maps_path, 
                class: "inline-flex justify-center py-2 px-4 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
          <%= form.submit "创建地图", 
                class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>
    </div>
  </div>
</div> 